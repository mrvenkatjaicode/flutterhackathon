import 'package:flutter/material.dart';

class StoryScreen3 extends StatelessWidget {
  const StoryScreen3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Story Screen Three")],
      ),
    );
  }
}
